version: '3'
volumes:
  user-data:
services:
  db_payments:
    image: mysql:8.0
    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: payments_db
      MYSQL_USER: payments_user
      MYSQL_PASSWORD: payments_pwd
    ports:
      - "3308:3306"
#
#  app:
#    image: openjdk:8
#    restart: always
#    working_dir: /app
#    volumes:
#    - ../application/build/libs:/app
#    - ~/.m2:/root/.m2
#    expose:
#    - "8080"
#    command:  java -jar register-api-application-1.0.0-REALEASE.jar
#    depends_on:
#    - db_payments
#    ports:
#    - "8080:8080"